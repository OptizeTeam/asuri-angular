import { FillTemplateDirective } from './fill-template.directive';

describe('FillTemplateDirective', () => {
  it('should create an instance', () => {
    const directive = new FillTemplateDirective();
    expect(directive).toBeTruthy();
  });
});
