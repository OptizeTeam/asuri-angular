import { MarkAsErrorDirective } from './mark-as-error.directive';

describe('MarkAsErrorDirective', () => {
  it('should create an instance', () => {
    const directive = new MarkAsErrorDirective();
    expect(directive).toBeTruthy();
  });
});
