import { PaintItBlackDirective } from './paint-it-black.directive';

describe('PaintItBlackDirective', () => {
  it('should create an instance', () => {
    const directive = new PaintItBlackDirective();
    expect(directive).toBeTruthy();
  });
});
